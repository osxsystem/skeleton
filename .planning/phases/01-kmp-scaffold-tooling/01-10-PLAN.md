---
phase: 01-kmp-scaffold-tooling
plan: "10"
type: execute
wave: 4
depends_on:
  - "01-09"
files_modified:
  - architecture.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements:
  - SCAF-01
  - SCAF-02
  - SCAF-03

must_haves:
  truths:
    - "architecture.md contains a note that the single shared/ module shape is superseded by the 3-module split and points at REQUIREMENTS.md SCAF-01 as the authority — D-21"
    - "docs/ARCHITECTURE.md contains the same reconciliation note — D-21"
    - "Neither file has been rewritten — only a targeted append/update to the module layout section — D-21"
    - "The files still describe MVVM + StateFlow + UDF as the pattern — unchanged"
  artifacts:
    - path: "architecture.md"
      provides: "Reconciled architecture doc noting 3-module shape"
      contains: "SCAF-01"
    - path: "docs/ARCHITECTURE.md"
      provides: "Reconciled companion doc noting 3-module shape"
      contains: "SCAF-01"
  key_links:
    - from: "architecture.md module layout section"
      to: ".planning/REQUIREMENTS.md SCAF-01"
      via: "inline reference note"
      pattern: "SCAF-01"
---

<objective>
Reconcile architecture.md and docs/ARCHITECTURE.md to acknowledge that the single-shared/ shape described in those files is superseded by the 3-module split locked in Phase 1.

Purpose: D-21 requires a one-line update pointing at REQUIREMENTS.md SCAF-01 as canonical. Without this, future contributors reading architecture.md would see conflicting descriptions of the module structure and potentially regress to a single-module shape.

Output: Minimal targeted updates to the module layout sections in both files. No rewrites, no new sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kmp-scaffold-tooling/01-CONTEXT.md
@architecture.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append multi-module reconciliation note to architecture.md and docs/ARCHITECTURE.md</name>
  <files>
    architecture.md
    docs/ARCHITECTURE.md
  </files>

  <read_first>
    - architecture.md — read the full file; identify the "Project layout" section and any "shared/" references
    - docs/ARCHITECTURE.md — read the full file; identify the "Directory structure" section
    - .planning/phases/01-kmp-scaffold-tooling/01-CONTEXT.md — D-21 (exact requirement: one-line note pointing at SCAF-01)
  </read_first>

  <action>
**architecture.md:**

Find the section that describes the project layout (`## Project layout (relevant slice)`) and the `shared/` entry. After the existing layout block, ADD the following note (do not replace or rewrite the existing block):

```markdown
> **Multi-module shape (Phase 1 update — D-21):**
> The `shared/` directory above reflects the original single-module sketch.
> The implemented layout splits into **three KMP modules**:
> `:shared-core` (DI, Ktor, SQLDelight, base repos), `:shared-components` (component ViewModels + expect/actual services),
> and `:shared-app` (showcase-only wiring, never published).
> See `.planning/REQUIREMENTS.md` **SCAF-01** for the authoritative module shape.
> The MVVM + StateFlow + UDF pattern and `IosViewModelStoreOwner` contract remain unchanged.
```

**docs/ARCHITECTURE.md:**

Find the `## Directory structure rationale` section. After the code block showing the `shared/` tree, ADD:

```markdown
> **Multi-module update (Phase 1):**
> The `shared/` single-module layout above is the original design sketch.
> Phase 1 implemented the three-module split mandated by **SCAF-01** (see `.planning/REQUIREMENTS.md`):
> `:shared-core`, `:shared-components`, `:shared-app`.
> All architectural principles (MVVM, StateFlow, UDF, IosViewModelStoreOwner) remain unchanged.
```

IMPORTANT: Do not rewrite either file. Only append these note blocks immediately after the relevant layout sections. The existing content is authoritative for the MVVM pattern description.
  </action>

  <verify>
    <automated>grep 'SCAF-01' architecture.md && grep 'SCAF-01' docs/ARCHITECTURE.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep 'SCAF-01' architecture.md` returns a match (D-21)
    - `grep 'SCAF-01' docs/ARCHITECTURE.md` returns a match (D-21)
    - `grep 'three-module\|three KMP modules\|3-module' architecture.md` returns a match (D-21)
    - `grep 'MVVM' architecture.md` still returns multiple matches (pattern description not removed)
    - `grep 'StateFlow' architecture.md` still returns multiple matches (unchanged)
    - `wc -l architecture.md` output is LARGER than before this task (lines added, not replaced)
  </acceptance_criteria>

  <done>architecture.md and docs/ARCHITECTURE.md each contain a note acknowledging the 3-module split and pointing at SCAF-01 as canonical authority</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files | Static markdown; no executable code; no trust boundary relevant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Repudiation | architecture.md diverging from implementation | mitigate | D-21 note points directly at REQUIREMENTS.md SCAF-01; future maintainers have a canonical reference |

No code changes, no runtime surface, no security concerns.
</threat_model>

<verification>
After executing this plan:
1. `grep 'SCAF-01' architecture.md` → match
2. `grep 'SCAF-01' docs/ARCHITECTURE.md` → match
3. `grep 'MVVM' architecture.md | wc -l` → same count as before (nothing removed)
</verification>

<success_criteria>
- architecture.md has a reconciliation note pointing at SCAF-01 (D-21 / SCAF-01)
- docs/ARCHITECTURE.md has the same reconciliation note (D-21)
- No existing content removed — the MVVM/StateFlow/UDF descriptions are intact
- Both files describe the correct 3-module structure as the implemented shape
</success_criteria>

<output>
After completion, create `.planning/phases/01-kmp-scaffold-tooling/01-10-SUMMARY.md` following the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
