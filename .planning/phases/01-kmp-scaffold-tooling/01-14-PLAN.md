---
phase: 01-kmp-scaffold-tooling
plan: "14"
type: execute
wave: 2
depends_on:
  - "01-11"
  - "01-12"
  - "01-13"
files_modified:
  - .planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md
autonomous: false
requirements:
  - SCAF-02
  - SCAF-05

must_haves:
  truths:
    - "androidApp assembleDebug exits 0 after CR-01/CR-02/CR-05/WR-01 fixes (confirms fixes did not regress Android build)"
    - "01-HUMAN-UAT.md is updated with post-fix instructions and Android smoke result"
    - "01-HUMAN-UAT.md contains a `## Post Gap-Closure Status` section listing the 5 closed defects (CR-01, CR-02, CR-05, WR-01, WR-05) with one line each indicating the closing plan ID."
    - "SCAF-02 runtime status (Xcode build + iOS simulator smoke) is recorded in 01-HUMAN-UAT.md"
    - "SCAF-05 runtime status (deinit log on navigation pop) is recorded in 01-HUMAN-UAT.md"
  artifacts:
    - path: ".planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md"
      provides: "Updated UAT tracker with post-fix status for all 7 tests + Android smoke result"
  key_links:
    - from: "androidApp assembleDebug"
      to: "dev.viethung.skeleton.android applicationId"
      via: "gradlew :androidApp:assembleDebug"
      pattern: "dev.viethung.skeleton.android"
---

<objective>
Coordinate the final runtime verification handoff for Phase 1. This plan has two parts:
1. An autonomous Android smoke test the executor CAN run immediately to confirm the gap-closure fixes did not break the Android build.
2. A human-action checkpoint for the iOS HUMAN-UAT (7 pending tests that require Xcode 16 on macOS with a simulator).

Purpose: Plans 01-11..01-13 fix all autonomous gaps. SCAF-02 (Xcode project + iOS simulator) and the SCAF-05 runtime deinit smoke still require a human on a Mac. This plan documents what the human needs to do and provides a pre-flight Android smoke check to confirm DI fixes are clean before manual iOS testing begins.

Output: Android assembleDebug green confirmation; 01-HUMAN-UAT.md updated with post-fix status after iOS testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kmp-scaffold-tooling/01-CONTEXT.md
@.planning/phases/01-kmp-scaffold-tooling/01-11-SUMMARY.md
@.planning/phases/01-kmp-scaffold-tooling/01-12-SUMMARY.md
@.planning/phases/01-kmp-scaffold-tooling/01-13-SUMMARY.md
@.planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android assembleDebug smoke test — confirm gap-closure fixes compile clean</name>
  <files>
    .planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md
  </files>

  <read_first>
    - .planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md (current state — 7 tests all pending)
    - .planning/phases/01-kmp-scaffold-tooling/01-11-SUMMARY.md (GreetingRepositoryImpl + initKoin fix)
    - .planning/phases/01-kmp-scaffold-tooling/01-12-SUMMARY.md (GreetingViewModelFactory fix)
    - .planning/phases/01-kmp-scaffold-tooling/01-13-SUMMARY.md (kotlinx-datetime removal + CI hardening)
  </read_first>

  <action>
**Step 1: Run Android assembleDebug and shared module builds**

```bash
./gradlew \
  :shared-core:build \
  :shared-components:build \
  :shared-app:build \
  :androidApp:assembleDebug \
  --no-daemon \
  --stacktrace \
  2>&1 | tee /tmp/android-smoke.log
```

Expected: exits 0 with no compilation errors.

**Step 2: Verify applicationId in the built APK manifest**

```bash
# Find the assembled debug APK
APK=$(find androidApp/build/outputs/apk/debug -name "*.apk" | head -1)
if [ -z "$APK" ]; then
  echo "ERROR: No APK found under androidApp/build/outputs/apk/debug"
  exit 1
fi
echo "APK found: $APK"
# Verify applicationId (confirms manifest was processed correctly)
# On CI this uses aapt2; locally use aapt or zipalign check
if command -v aapt2 >/dev/null 2>&1; then
  aapt2 dump badging "$APK" | grep 'package: name'
elif command -v aapt >/dev/null 2>&1; then
  aapt dump badging "$APK" | grep 'package: name'
else
  echo "SKIP: aapt/aapt2 not available — APK file exists at: $APK (build succeeded)"
fi
```

**Step 3: Update 01-HUMAN-UAT.md with Android smoke result and post-fix context**

Update the `## Current Test` section and add a new `## Android Smoke (Post Gap-Closure)` section to record the result. Also add a `## Post Gap-Closure Status` section that tells the human what changed in 01-11..01-13 before they start iOS testing.

The updated file should reflect:
- Android smoke: PASSED or FAILED (fill in actual result)
- Note that CR-01 (GreetingRepositoryImpl), CR-02 (iosPlatformModule + vararg initKoin), CR-05 (factory construct not get), WR-01 (datetime removed), WR-05 (CI hardened) are all fixed
- iOS tests 1-7 remain PENDING (requires human on Mac + Xcode 16)
- The iOS DI fix (iosPlatformModule passed to doInitKoin) is in place — iOS runtime test should now succeed if SKIE name mangling is correct (IN-03 note: verify `doInitKoin` vs `initKoin` in the generated header)

Write the following to 01-HUMAN-UAT.md (updating the existing structure, not replacing it):

```markdown
---
status: partial
phase: 01-kmp-scaffold-tooling
source: [01-08-PLAN.md, 01-08-SUMMARY.md, 01-11-PLAN.md, 01-12-PLAN.md, 01-13-PLAN.md]
started: 2026-05-09T00:00:00Z
updated: [INSERT TODAY'S DATE]
---

## Current Test

Android smoke test (assembleDebug): [PASSED/FAILED — fill in actual result]
iOS tests 1-7: pending human on Mac + Xcode 16

## Post Gap-Closure Status (Plans 01-11..01-13)

The following blockers are now fixed — re-run iOS tests with the updated codebase:

| Fix | Gap | What Changed |
|-----|-----|-------------|
| CR-01 | GreetingRepository never bound | GreetingRepositoryImpl.kt created; factory<GreetingRepository> { GreetingRepositoryImpl(get()) } added to appModule |
| CR-02 | iOS DatabaseDriverFactory unbound | iosPlatformModule created in shared-app/iosMain; initKoin(vararg platformModules) now accepts platform modules; AppKoinBridge passes iosPlatformModule |
| CR-05 | GreetingViewModelFactory bypassed ViewModelStore | Factory now constructs GreetingViewModel(useCase) directly; ViewModelStore owns instance |
| WR-01 | kotlinx-datetime prohibited dependency | Removed from shared-core/build.gradle.kts and gradle/libs.versions.toml |
| WR-05 | CI SKIE gate exited 0 on missing headers | ci.yml gate now exits 1 with ERROR message |

## Android Smoke (Post Gap-Closure)

**Command:** `./gradlew :shared-core:build :shared-components:build :shared-app:build :androidApp:assembleDebug --no-daemon`
**Result:** [PASSED/FAILED]
**Notes:** [any build warnings or issues]

## iOS Pre-Flight Checklist (Before Starting HUMAN-UAT)

Before running the 7 iOS tests below, confirm:
- [ ] Plans 01-11, 01-12, 01-13 are all COMPLETE (check SUMMARY files)
- [ ] Android smoke above is PASSED
- [ ] XCFramework has been rebuilt after the CR-02 fix: `./gradlew :shared-components:assembleSKIEDebugXCFramework`
- [ ] IN-03 note: in the generated SkeletonKit.framework/Headers, verify the Swift symbol name for `initKoin`. SKIE may generate `doInitKoin` or `initKoin`. AppKoinBridge.swift calls `AppModuleKt.doInitKoin(platformModules:)` — update if the header shows a different name.

## Tests

### 1. Build the SkeletonKit XCFramework
expected: `./gradlew :shared-components:assembleSkeletonKitReleaseXCFramework` succeeds; the framework appears at `shared-components/build/XCFrameworks/release/SkeletonKit.xcframework`.
result: pending

### 2. Create the iosApp Xcode project
expected: Xcode → File → New → Project → iOS App (SwiftUI). Product Name `iosApp`, Bundle ID `dev.viethung.skeleton.ios`. Saved into `iosApp/` so `iosApp/iosApp.xcodeproj` exists. Minimum deployment target = iOS 17.0.
result: pending

### 3. Wire committed Swift sources into the project
expected: Drag `iosApp/iosApp/Common/IosViewModelStoreOwner.swift`, `iosApp/iosApp/App/AppKoinBridge.swift`, `iosApp/iosApp/Greeting/GreetingScreen.swift` into the project navigator. Replace Xcode-generated `iosApp.swift` and `ContentView.swift` with the committed versions.
result: pending

### 4. Embed SkeletonKit framework
expected: Targets → iosApp → General → Frameworks, Libraries, and Embedded Content → + → add `SkeletonKit.xcframework` → set to Embed & Sign.
result: pending

### 5. Add `-lsqlite3` linker flag
expected: Build Settings → Other Linker Flags → add `-lsqlite3`.
result: pending

### 6. Compile and run on iOS 17 simulator
expected: ⌘B compiles green. Run on iPhone 15 / iOS 17 simulator. "Hello, KMP" renders on screen (CR-01 + CR-02 now fixed — Koin should resolve fully).
result: pending

### 7. Verify @StateObject deinit lifecycle
expected: Push a second view (`NavigationLink` to a trivial `Text("ok")` view), then pop back. Xcode console must show `[IosViewModelStoreOwner] deinit cleared store`. This proves D-12 / Pitfall 1+2 / SCAF-05 are working at runtime. CR-05 fix ensures the correct VM instance receives onCleared().
result: pending

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 0

## Gaps

(Fill in after running tests on Mac + Xcode 16. The CR-01/CR-02/CR-05 fixes should unblock tests 6 and 7.)
```
  </action>

  <verify>
    <automated># Long-running (~5-10 min cold cache) — intended gate; do not shortcut.
    ./gradlew :shared-core:build :shared-components:build :shared-app:build :androidApp:assembleDebug --no-daemon 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `./gradlew :shared-core:build :shared-components:build :shared-app:build :androidApp:assembleDebug --no-daemon` exits 0
    - `find androidApp/build/outputs/apk/debug -name '*.apk' | wc -l` returns at least 1
    - `grep 'Post Gap-Closure Status' .planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md` returns a match
    - `grep 'CR-01.*01-11\|01-11.*CR-01' .planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md` returns a match (Post Gap-Closure Status table has CR-01 row with closing plan ID)
    - `grep 'Android Smoke' .planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md` returns a match
    - `grep 'iOS Pre-Flight Checklist' .planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md` returns a match
  </acceptance_criteria>

  <done>Android assembleDebug exits 0; 01-HUMAN-UAT.md updated with post-fix context and Android smoke result</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
Plans 01-11 (Koin DI graph closure: GreetingRepositoryImpl, vararg initKoin, IosPlatformModule, updated AppKoinBridge), 01-12 (GreetingViewModelFactory lifecycle fix), and 01-13 (CI hardening, kotlinx-datetime removal) have been executed. Android assembleDebug is confirmed green (Task 1 above).

The iOS codebase is now in the correct state for first-time Xcode project creation and manual UAT.
  </what-built>

  <how-to-verify>
On a Mac with Xcode 16 installed, run the 7 tests in `.planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md` in order:

**Pre-flight (run first):**
```bash
./gradlew :shared-components:assembleSkeletonKitReleaseXCFramework
# Verify: shared-components/build/XCFrameworks/release/SkeletonKit.xcframework exists
```

**SKIE name check (critical for Test 6):**
After the XCFramework builds, inspect the generated header:
```bash
find shared-components/build/XCFrameworks -name "*.h" | xargs grep -l 'initKoin\|doInitKoin' 2>/dev/null
```
Open the matching header and verify the Swift symbol name for `initKoin`. If it is `initKoin` (not `doInitKoin`), update `iosApp/iosApp/App/AppKoinBridge.swift` line calling `doInitKoin` to match before building the Xcode project. (This is IN-03 from the code review.)

**Test 2 — Create Xcode project:**
Xcode → File → New → Project → iOS App (SwiftUI)
- Product Name: `iosApp`
- Bundle ID: `dev.viethung.skeleton.ios`
- Save location: `iosApp/` (so `iosApp/iosApp.xcodeproj` is created)
- Minimum Deployment: iOS 17.0

**Test 3 — Wire Swift sources:**
In project navigator, drag in:
- `iosApp/iosApp/Common/IosViewModelStoreOwner.swift`
- `iosApp/iosApp/App/AppKoinBridge.swift`
- `iosApp/iosApp/Greeting/GreetingScreen.swift`
Replace generated `iosApp.swift` and `ContentView.swift` with the committed versions.

**Test 4 — Embed framework:**
Target → General → Frameworks → + → `SkeletonKit.xcframework` → Embed & Sign

**Test 5 — Linker flag:**
Target → Build Settings → Other Linker Flags → add `-lsqlite3`

**Test 6 — Build and run (⌘R) on iPhone 15 / iOS 17 simulator:**
Expected: "Hello, KMP" renders on screen with no Koin crash in console.

**Test 7 — deinit lifecycle smoke:**
Add a `NavigationLink` to a second view, push it, then pop back.
Expected: `[IosViewModelStoreOwner] deinit cleared store` appears in Xcode console.

After completing all 7 tests, update `01-HUMAN-UAT.md`:
- Set each test result to `passed` or describe the issue
- Update the Summary block (passed/issues counts)
- Fill in the Gaps section with any new issues found
  </how-to-verify>

  <resume-signal>
Type "ios-uat-complete" after updating 01-HUMAN-UAT.md, or describe any blocking issues found during testing. If SKIE name mangling for initKoin differs from doInitKoin, also report the correct symbol name so AppKoinBridge.swift can be updated.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human reporter → UAT results | The human must report test outcomes truthfully; this plan cannot mechanically verify iOS simulator behavior |
| Local build → CI | assembleDebug passing locally does not guarantee CI passes (JDK version, SDK availability), but it validates the Kotlin compilation and Koin wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Repudiation | Human UAT reporting | accept | Manual testing requires trust; the HUMAN-UAT.md is the paper trail; if results are inaccurate, Phase 2 will surface regressions at runtime |
| T-14-02 | Denial of Service | assembleDebug fails after CR-02 fix | mitigate | The Gradle build step in Task 1 catches this immediately; if it fails, the executor must record the error and the human checkpoint must not proceed until the build is green |
| T-14-03 | Information Disclosure | SKIE header inspection reveals API surface | accept | The header inspection is for IN-03 symbol name verification only; the content (initKoin signature) is not sensitive |
</threat_model>

<verification>
After executing this plan:
1. `./gradlew :shared-core:build :shared-components:build :shared-app:build :androidApp:assembleDebug --no-daemon` → exit 0 (Task 1 autonomous gate)
2. `find androidApp/build/outputs/apk/debug -name '*.apk' | wc -l` → at least 1 (APK exists)
3. `grep 'Post Gap-Closure Status' .planning/phases/01-kmp-scaffold-tooling/01-HUMAN-UAT.md` → match
4. Human checkpoint provides iOS UAT results (resume after "ios-uat-complete" signal)
</verification>

<success_criteria>
- Android assembleDebug exits 0 — gap-closure fixes compile clean and do not regress Android (SCAF-01)
- 01-HUMAN-UAT.md updated with post-fix context for human tester (SCAF-02)
- Human completes 7 iOS HUMAN-UAT tests on Mac + Xcode 16 and records outcomes (SCAF-02, SCAF-05)
- SCAF-05 runtime deinit smoke (Test 7) result recorded — confirms CR-05 fix at runtime
</success_criteria>

<output>
After completion, create `.planning/phases/01-kmp-scaffold-tooling/01-14-SUMMARY.md` following the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
