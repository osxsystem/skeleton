---
phase: 02-design-token-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt
  - shared-core/src/commonTest/kotlin/dev/viethung/core/theme/DesignTokensTest.kt
autonomous: true
requirements:
  - THEME-01
  - THEME-02

must_haves:
  truths:
    - "DesignTokens.kt is in shared-core/src/commonMain and contains NO android.*, androidx.*, SwiftUI, or UIKit imports — enforced by KMP compiler (CLAUDE.md rule, D-01)"
    - "Every color constant in LightColors and DarkColors is declared as `const val <name>: Long = 0xFF...L` — the L suffix is load-bearing (D-02, D-15, Pitfall 6)"
    - "LightColors defines all ~35 M3 ColorScheme roles listed in D-01 as Long constants"
    - "DarkColors defines all ~35 M3 ColorScheme roles listed in D-01 as Long constants"
    - "TextStyleToken data class has exactly four fields: size: Float, weight: Int, lineHeight: Float, letterSpacing: Float — no Compose/SwiftUI types (D-04)"
    - "All 15 M3 typography roles are present in DesignTokens.typography (D-05)"
    - "Spacing has 7 semantic steps: xxs=2f, xs=4f, sm=8f, md=16f, lg=24f, xl=32f, xxl=48f (D-09)"
    - "Radius has 7 semantic steps: none=0f, xs=4f, sm=8f, md=12f, lg=16f, xl=28f, full=9999f (D-10)"
    - "noColorConstantIsNegative() commonTest passes on JVM target — catches Pitfall 6 (D-15)"
    - "DesignTokensTest uses kotlin.test.Test (NEVER org.junit.Test) — required for iosSimulatorArm64 target (Pitfall 18, D-17 from Phase 1)"
  artifacts:
    - path: "shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt"
      provides: "Single source of truth for all visual primitives"
      contains: "object LightColors"
      min_lines: 120
    - path: "shared-core/src/commonTest/kotlin/dev/viethung/core/theme/DesignTokensTest.kt"
      provides: "Pitfall 6 regression guard on both JVM and iosSimulatorArm64"
      contains: "noColorConstantIsNegative"
  key_links:
    - from: "DesignTokensTest.kt"
      to: "DesignTokens.LightColors + DesignTokens.DarkColors"
      via: "explicit List<Long> of all color constants"
      pattern: "assertTrue.*> 0L"
---

<objective>
Create `DesignTokens.kt` in `:shared-core/commonMain` and `DesignTokensTest.kt` in `:shared-core/commonTest`.

Purpose: This is the Wave 1 anchor for the entire phase. Every platform adapter (Compose and SwiftUI) in Wave 2 depends on the contracts defined here. DesignTokens is the single source of truth for all visual primitives — the KMP compiler enforces the primitives-only rule by rejecting Compose and SwiftUI imports on the opposing target.

Output: One Kotlin file with all color/typography/spacing/radius constants; one commonTest that acts as the Pitfall 6 regression guard.
</objective>

<execution_context>
@/Users/hugues_mini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hugues_mini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-design-token-bridge/02-CONTEXT.md
@.planning/phases/02-design-token-bridge/02-RESEARCH.md
@.planning/phases/02-design-token-bridge/02-PATTERNS.md
@.planning/phases/01-kmp-scaffold-tooling/01-CONTEXT.md

<interfaces>
<!-- Established package paths from Phase 1 (01-CONTEXT.md D-03, D-04) -->
<!-- All shared-core commonMain files use: package dev.viethung.core.* -->
<!-- DesignTokens goes in: shared-core/src/commonMain/kotlin/dev/viethung/core/theme/ -->
<!-- DesignTokensTest goes in: shared-core/src/commonTest/kotlin/dev/viethung/core/theme/ -->

<!-- Test import pattern from Phase 1 (SkieGenericsTest.kt — exact pattern to replicate): -->
```kotlin
import kotlin.test.Test        // NEVER org.junit.Test (Pitfall 18)
import kotlin.test.assertTrue
```

<!-- Nested object pattern from SampleUiState.kt (structural reference): -->
```kotlin
// SampleUiState.kt nesting idiom — DesignTokens uses same depth but objects not sealed
package dev.viethung.components
sealed interface SampleUiState { ... }
// Delta: DesignTokens uses `object DesignTokens { object LightColors { ... } }`
```

<!-- Pitfall 6 critical constraint: -->
```kotlin
// CORRECT — L suffix forces Long literal on all KMP targets
const val primary: Long = 0xFF3F51B5L
// WRONG — omitting L infers Int on JVM/Native, overflows to negative for any color
// where the high byte (alpha) is 0xFF (>= 0x80000000 as Int)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DesignTokens.kt — all visual primitives in commonMain</name>
  <files>shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt</files>
  <read_first>
    - .planning/phases/02-design-token-bridge/02-CONTEXT.md (D-01 through D-10 — locked decisions for every field value)
    - .planning/phases/02-design-token-bridge/02-PATTERNS.md (Pattern 1 section: DesignTokens.kt)
    - .planning/phases/02-design-token-bridge/02-RESEARCH.md (Pattern 1 section; §Pitfall 6 walkthrough)
    - shared-core/src/commonMain/kotlin/dev/viethung/components/SampleUiState.kt (structural nesting analog — read before writing)
  </read_first>
  <behavior>
    - DesignTokens.kt has NO imports at the top (no Compose, no SwiftUI, no android.*)
    - `object DesignTokens` compiles on both androidMain and iosMain targets without errors
    - LightColors.primary is a positive Long value (> 0L)
    - DarkColors.primary is a positive Long value (> 0L)
    - TextStyleToken(size=57f, weight=400, lineHeight=64f, letterSpacing=-0.25f).size == 57f
    - All 15 typography roles present in DesignTokens.typography object
    - DesignTokens.spacing.md == 16f
    - DesignTokens.radius.full == 9999f
  </behavior>
  <action>
Create `shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt` with the following exact content.

**Package and no-import rule (D-01, CLAUDE.md §2):** The file must have zero import statements. Every type used is a Kotlin primitive (Long, Float, Int) or a locally-declared data class.

**TextStyleToken data class (D-04):**
```kotlin
data class TextStyleToken(
    val size: Float,
    val weight: Int,        // 100=Thin, 300=Light, 400=Regular, 500=Medium, 600=SemiBold, 700=Bold
    val lineHeight: Float,  // in sp — Compose uses directly; iOS: lineHeight/size = lineSpacingMultiple
    val letterSpacing: Float, // in sp — iOS: divide by size to get tracking ratio for Font.leading
)
```

**object DesignTokens structure (D-01, D-02):**
```kotlin
object DesignTokens {
    object LightColors { ... }
    object DarkColors  { ... }
    object typography  { ... }
    object spacing     { ... }
    object radius      { ... }
}
```

**LightColors — Skeleton indigo/teal/neutral identity palette (D-03, D-15):**
Every constant MUST end with `L`. Use exactly these values:
```kotlin
object LightColors {
    const val primary: Long                 = 0xFF3F51B5L
    const val onPrimary: Long               = 0xFFFFFFFFL
    const val primaryContainer: Long        = 0xFFDBDEFFL
    const val onPrimaryContainer: Long      = 0xFF000964L
    const val secondary: Long               = 0xFF5C6BC0L
    const val onSecondary: Long             = 0xFFFFFFFFL
    const val secondaryContainer: Long      = 0xFFE2E5FFL
    const val onSecondaryContainer: Long    = 0xFF161B5CL
    const val tertiary: Long                = 0xFF009688L
    const val onTertiary: Long              = 0xFFFFFFFFL
    const val tertiaryContainer: Long       = 0xFFB2DFDB L
    const val onTertiaryContainer: Long     = 0xFF002826L
    const val error: Long                   = 0xFFB3261EL
    const val onError: Long                 = 0xFFFFFFFFL
    const val errorContainer: Long          = 0xFFF9DEDC L
    const val onErrorContainer: Long        = 0xFF410E0BL
    const val background: Long              = 0xFFFFFBFEL
    const val onBackground: Long            = 0xFF1C1B1FL
    const val surface: Long                 = 0xFFFFFBFEL
    const val onSurface: Long               = 0xFF1C1B1FL
    const val surfaceVariant: Long          = 0xFFE7E0ECL
    const val onSurfaceVariant: Long        = 0xFF49454FL
    const val outline: Long                 = 0xFF79747EL
    const val outlineVariant: Long          = 0xFFCAC4D0L
    const val scrim: Long                   = 0xFF000000L
    const val inverseSurface: Long          = 0xFF313033L
    const val inverseOnSurface: Long        = 0xFFF4EFF4L
    const val inversePrimary: Long          = 0xFFBBC2FFL
    const val surfaceDim: Long              = 0xFFDED8E1L
    const val surfaceBright: Long           = 0xFFFFFBFEL
    const val surfaceContainerLowest: Long  = 0xFFFFFFFFL
    const val surfaceContainerLow: Long     = 0xFFF7F2FAL
    const val surfaceContainer: Long        = 0xFFF3EDF7L
    const val surfaceContainerHigh: Long    = 0xFFECE6F0L
    const val surfaceContainerHighest: Long = 0xFFE6E0E9L
}
```

NOTE: Remove any spaces inside the hex literals — `0xFFB2DFDB L` above shows the pattern; the actual file must have `0xFFB2DFDB L` written as `0xFFB2DFDBL` (no space before L). Similarly `0xFFF9DEDC L` → `0xFFF9DEDCL`. The space in this action text is a formatting artifact — the literal must be `0xFF...L` with no space.

**DarkColors — dark variant of the same palette (D-03, D-15):**
```kotlin
object DarkColors {
    const val primary: Long                 = 0xFFBBC2FFL
    const val onPrimary: Long               = 0xFF0D1888L
    const val primaryContainer: Long        = 0xFF253099L
    const val onPrimaryContainer: Long      = 0xFFDBDEFFL
    const val secondary: Long               = 0xFFC1C8FFL
    const val onSecondary: Long             = 0xFF2B317AL
    const val secondaryContainer: Long      = 0xFF424791L
    const val onSecondaryContainer: Long    = 0xFFE2E5FFL
    const val tertiary: Long                = 0xFF80CBC4L
    const val onTertiary: Long              = 0xFF00403BL
    const val tertiaryContainer: Long       = 0xFF005951L
    const val onTertiaryContainer: Long     = 0xFFB2DFDBL
    const val error: Long                   = 0xFFF2B8B5L
    const val onError: Long                 = 0xFF601410L
    const val errorContainer: Long          = 0xFF8C1D18L
    const val onErrorContainer: Long        = 0xFFF9DEDCL
    const val background: Long              = 0xFF1C1B1FL
    const val onBackground: Long            = 0xFFE6E1E5L
    const val surface: Long                 = 0xFF1C1B1FL
    const val onSurface: Long               = 0xFFE6E1E5L
    const val surfaceVariant: Long          = 0xFF49454FL
    const val onSurfaceVariant: Long        = 0xFFCAC4D0L
    const val outline: Long                 = 0xFF938F99L
    const val outlineVariant: Long          = 0xFF49454FL
    const val scrim: Long                   = 0xFF000000L
    const val inverseSurface: Long          = 0xFFE6E1E5L
    const val inverseOnSurface: Long        = 0xFF313033L
    const val inversePrimary: Long          = 0xFF3F51B5L
    const val surfaceDim: Long              = 0xFF1C1B1FL
    const val surfaceBright: Long           = 0xFF3B383EL
    const val surfaceContainerLowest: Long  = 0xFF0F0D13L
    const val surfaceContainerLow: Long     = 0xFF1D1B20L
    const val surfaceContainer: Long        = 0xFF211F26L
    const val surfaceContainerHigh: Long    = 0xFF2B2930L
    const val surfaceContainerHighest: Long = 0xFF36343BL
}
```

**Typography — all 15 M3 roles (D-05), M3 default spec values:**
```kotlin
object typography {
    val displayLarge  = TextStyleToken(size = 57f, weight = 400, lineHeight = 64f,  letterSpacing = -0.25f)
    val displayMedium = TextStyleToken(size = 45f, weight = 400, lineHeight = 52f,  letterSpacing = 0f)
    val displaySmall  = TextStyleToken(size = 36f, weight = 400, lineHeight = 44f,  letterSpacing = 0f)
    val headlineLarge  = TextStyleToken(size = 32f, weight = 400, lineHeight = 40f, letterSpacing = 0f)
    val headlineMedium = TextStyleToken(size = 28f, weight = 400, lineHeight = 36f, letterSpacing = 0f)
    val headlineSmall  = TextStyleToken(size = 24f, weight = 400, lineHeight = 32f, letterSpacing = 0f)
    val titleLarge  = TextStyleToken(size = 22f, weight = 400, lineHeight = 28f, letterSpacing = 0f)
    val titleMedium = TextStyleToken(size = 16f, weight = 500, lineHeight = 24f, letterSpacing = 0.15f)
    val titleSmall  = TextStyleToken(size = 14f, weight = 500, lineHeight = 20f, letterSpacing = 0.1f)
    val bodyLarge   = TextStyleToken(size = 16f, weight = 400, lineHeight = 24f, letterSpacing = 0.5f)
    val bodyMedium  = TextStyleToken(size = 14f, weight = 400, lineHeight = 20f, letterSpacing = 0.25f)
    val bodySmall   = TextStyleToken(size = 12f, weight = 400, lineHeight = 16f, letterSpacing = 0.4f)
    val labelLarge  = TextStyleToken(size = 14f, weight = 500, lineHeight = 20f, letterSpacing = 0.1f)
    val labelMedium = TextStyleToken(size = 12f, weight = 500, lineHeight = 16f, letterSpacing = 0.5f)
    val labelSmall  = TextStyleToken(size = 11f, weight = 500, lineHeight = 16f, letterSpacing = 0.5f)
}
```

**Spacing — 7 semantic steps (D-09):**
```kotlin
object spacing {
    const val xxs: Float = 2f
    const val xs:  Float = 4f
    const val sm:  Float = 8f
    const val md:  Float = 16f
    const val lg:  Float = 24f
    const val xl:  Float = 32f
    const val xxl: Float = 48f
}
```

**Radius — 7 semantic steps (D-10):**
```kotlin
object radius {
    const val none: Float = 0f
    const val xs:   Float = 4f
    const val sm:   Float = 8f
    const val md:   Float = 12f
    const val lg:   Float = 16f
    const val xl:   Float = 28f
    const val full: Float = 9999f
}
```
  </action>
  <verify>
    <automated>./gradlew :shared-core:jvmTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import ' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt` returns 0 (no imports anywhere in the file)
    - `grep -c 'object LightColors' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt` returns 1
    - `grep -c 'object DarkColors' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt` returns 1
    - `grep -v '^[[:space:]]*//' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt | grep -c ': Long = 0x'` returns >= 70 (35 LightColors + 35 DarkColors constants)
    - Every Long constant line ends with `L` before any trailing comment — verified by: `grep ': Long = 0x' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt | grep -v 'L[[:space:]]*$\|L[[:space:]]*//'` returns 0 hits (all have L suffix)
    - `grep -c 'val displayLarge\|val displayMedium\|val displaySmall\|val headlineLarge\|val headlineMedium\|val headlineSmall\|val titleLarge\|val titleMedium\|val titleSmall\|val bodyLarge\|val bodyMedium\|val bodySmall\|val labelLarge\|val labelMedium\|val labelSmall' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt` returns 15
    - `grep -c 'data class TextStyleToken' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt` returns 1
    - `./gradlew :shared-core:jvmTest` exits 0 (file compiles and JVM tests pass)
  </acceptance_criteria>
  <done>DesignTokens.kt exists at the correct path, has zero imports, contains LightColors and DarkColors with 35 Long constants each (all with L suffix), TextStyleToken data class with four primitive fields, 15 typography roles, 7 spacing steps, and 7 radius steps. JVM compilation is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DesignTokensTest.kt — Pitfall 6 regression guard + completeness assertions</name>
  <files>shared-core/src/commonTest/kotlin/dev/viethung/core/theme/DesignTokensTest.kt</files>
  <read_first>
    - shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt (the file being tested — must read first to build the explicit list)
    - .planning/phases/02-design-token-bridge/02-PATTERNS.md (DesignTokensTest pattern section — explicit-list assertion pattern)
    - .planning/phases/02-design-token-bridge/02-VALIDATION.md (Wave 0 requirements — noColorConstantIsNegative)
    - shared-components/src/commonTest/kotlin/dev/viethung/components/SkieGenericsTest.kt (exact import pattern analog — kotlin.test.Test usage)
  </read_first>
  <behavior>
    - noColorConstantIsNegative() fails compilation (Red) if any color constant is declared without L suffix or as Int
    - noColorConstantIsNegative() passes (Green) when all 70 color constants are positive Long values
    - typographyRolesAreComplete() passes when all 15 M3 roles are present and sizes are > 0f
    - spacingIsOrdered() passes when xxs < xs < sm < md < lg < xl < xxl
  </behavior>
  <action>
Create `shared-core/src/commonTest/kotlin/dev/viethung/core/theme/DesignTokensTest.kt`.

**Import pattern — copy exactly from SkieGenericsTest.kt (Pitfall 18 / D-17):**
```kotlin
package dev.viethung.core.theme

import kotlin.test.Test
import kotlin.test.assertTrue
import kotlin.test.assertEquals
```
Use `kotlin.test.Test` — NEVER `org.junit.Test`. This file runs on both JVM and `iosSimulatorArm64` targets; JUnit does not exist on the KMP native target.

**Test class shape:**
```kotlin
class DesignTokensTest {
```

**Test 1 — noColorConstantIsNegative() — Pitfall 6 mitigation (D-15, D-02):**
Use an EXPLICIT LIST (not reflection — reflection is unreliable on iosSimulatorArm64).
The list must contain every constant from both LightColors and DarkColors.
```kotlin
    @Test
    fun noColorConstantIsNegative() {
        val lightColors: List<Long> = listOf(
            DesignTokens.LightColors.primary,
            DesignTokens.LightColors.onPrimary,
            DesignTokens.LightColors.primaryContainer,
            DesignTokens.LightColors.onPrimaryContainer,
            DesignTokens.LightColors.secondary,
            DesignTokens.LightColors.onSecondary,
            DesignTokens.LightColors.secondaryContainer,
            DesignTokens.LightColors.onSecondaryContainer,
            DesignTokens.LightColors.tertiary,
            DesignTokens.LightColors.onTertiary,
            DesignTokens.LightColors.tertiaryContainer,
            DesignTokens.LightColors.onTertiaryContainer,
            DesignTokens.LightColors.error,
            DesignTokens.LightColors.onError,
            DesignTokens.LightColors.errorContainer,
            DesignTokens.LightColors.onErrorContainer,
            DesignTokens.LightColors.background,
            DesignTokens.LightColors.onBackground,
            DesignTokens.LightColors.surface,
            DesignTokens.LightColors.onSurface,
            DesignTokens.LightColors.surfaceVariant,
            DesignTokens.LightColors.onSurfaceVariant,
            DesignTokens.LightColors.outline,
            DesignTokens.LightColors.outlineVariant,
            DesignTokens.LightColors.scrim,
            DesignTokens.LightColors.inverseSurface,
            DesignTokens.LightColors.inverseOnSurface,
            DesignTokens.LightColors.inversePrimary,
            DesignTokens.LightColors.surfaceDim,
            DesignTokens.LightColors.surfaceBright,
            DesignTokens.LightColors.surfaceContainerLowest,
            DesignTokens.LightColors.surfaceContainerLow,
            DesignTokens.LightColors.surfaceContainer,
            DesignTokens.LightColors.surfaceContainerHigh,
            DesignTokens.LightColors.surfaceContainerHighest,
        )
        val darkColors: List<Long> = listOf(
            DesignTokens.DarkColors.primary,
            DesignTokens.DarkColors.onPrimary,
            DesignTokens.DarkColors.primaryContainer,
            DesignTokens.DarkColors.onPrimaryContainer,
            DesignTokens.DarkColors.secondary,
            DesignTokens.DarkColors.onSecondary,
            DesignTokens.DarkColors.secondaryContainer,
            DesignTokens.DarkColors.onSecondaryContainer,
            DesignTokens.DarkColors.tertiary,
            DesignTokens.DarkColors.onTertiary,
            DesignTokens.DarkColors.tertiaryContainer,
            DesignTokens.DarkColors.onTertiaryContainer,
            DesignTokens.DarkColors.error,
            DesignTokens.DarkColors.onError,
            DesignTokens.DarkColors.errorContainer,
            DesignTokens.DarkColors.onErrorContainer,
            DesignTokens.DarkColors.background,
            DesignTokens.DarkColors.onBackground,
            DesignTokens.DarkColors.surface,
            DesignTokens.DarkColors.onSurface,
            DesignTokens.DarkColors.surfaceVariant,
            DesignTokens.DarkColors.onSurfaceVariant,
            DesignTokens.DarkColors.outline,
            DesignTokens.DarkColors.outlineVariant,
            DesignTokens.DarkColors.scrim,
            DesignTokens.DarkColors.inverseSurface,
            DesignTokens.DarkColors.inverseOnSurface,
            DesignTokens.DarkColors.inversePrimary,
            DesignTokens.DarkColors.surfaceDim,
            DesignTokens.DarkColors.surfaceBright,
            DesignTokens.DarkColors.surfaceContainerLowest,
            DesignTokens.DarkColors.surfaceContainerLow,
            DesignTokens.DarkColors.surfaceContainer,
            DesignTokens.DarkColors.surfaceContainerHigh,
            DesignTokens.DarkColors.surfaceContainerHighest,
        )
        (lightColors + darkColors).forEachIndexed { index, color ->
            assertTrue(
                color > 0L,
                "Color constant at index $index is $color (negative). " +
                "Missing L suffix on a 0xFF... constant — see Pitfall 6 in .planning/research/PITFALLS.md"
            )
        }
    }
```

**Test 2 — typographyRolesAreComplete() — THEME-01 completeness guard:**
```kotlin
    @Test
    fun typographyRolesAreComplete() {
        val roles: List<Float> = listOf(
            DesignTokens.typography.displayLarge.size,
            DesignTokens.typography.displayMedium.size,
            DesignTokens.typography.displaySmall.size,
            DesignTokens.typography.headlineLarge.size,
            DesignTokens.typography.headlineMedium.size,
            DesignTokens.typography.headlineSmall.size,
            DesignTokens.typography.titleLarge.size,
            DesignTokens.typography.titleMedium.size,
            DesignTokens.typography.titleSmall.size,
            DesignTokens.typography.bodyLarge.size,
            DesignTokens.typography.bodyMedium.size,
            DesignTokens.typography.bodySmall.size,
            DesignTokens.typography.labelLarge.size,
            DesignTokens.typography.labelMedium.size,
            DesignTokens.typography.labelSmall.size,
        )
        assertEquals(15, roles.size, "Expected 15 M3 typography roles")
        roles.forEachIndexed { index, size ->
            assertTrue(size > 0f, "Typography role at index $index has non-positive size $size")
        }
    }
```

**Test 3 — spacingIsOrdered() — D-09 constraint:**
```kotlin
    @Test
    fun spacingIsOrdered() {
        assertTrue(DesignTokens.spacing.xxs < DesignTokens.spacing.xs,  "xxs < xs")
        assertTrue(DesignTokens.spacing.xs  < DesignTokens.spacing.sm,  "xs < sm")
        assertTrue(DesignTokens.spacing.sm  < DesignTokens.spacing.md,  "sm < md")
        assertTrue(DesignTokens.spacing.md  < DesignTokens.spacing.lg,  "md < lg")
        assertTrue(DesignTokens.spacing.lg  < DesignTokens.spacing.xl,  "lg < xl")
        assertTrue(DesignTokens.spacing.xl  < DesignTokens.spacing.xxl, "xl < xxl")
    }
```

Close the class: `}`
  </action>
  <verify>
    <automated>./gradlew :shared-core:jvmTest --tests "dev.viethung.core.theme.DesignTokensTest" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'org.junit.Test' shared-core/src/commonTest/kotlin/dev/viethung/core/theme/DesignTokensTest.kt` returns 0 (JUnit import forbidden)
    - `grep -c 'kotlin.test.Test' shared-core/src/commonTest/kotlin/dev/viethung/core/theme/DesignTokensTest.kt` returns 1
    - `grep -c 'noColorConstantIsNegative\|typographyRolesAreComplete\|spacingIsOrdered' shared-core/src/commonTest/kotlin/dev/viethung/core/theme/DesignTokensTest.kt` returns 3
    - `./gradlew :shared-core:jvmTest --tests "dev.viethung.core.theme.DesignTokensTest"` exits 0 with "3 tests completed, 0 failed"
    - `./gradlew :shared-core:allTests` exits 0 (runs on both JVM and iosSimulatorArm64 — catches platform-specific overflow; may take 30-60s)
  </acceptance_criteria>
  <done>DesignTokensTest.kt passes on both JVM and iosSimulatorArm64 targets. `./gradlew :shared-core:allTests` is green. The noColorConstantIsNegative test covers all 70 color constants (35 light + 35 dark).</done>
</task>

</tasks>

<threat_model>
No security-relevant surface — pure design-token primitives with no I/O, no user input, no network, no auth. Pitfall 6 (ARGB Long overflow) is tracked as a correctness threat: if any color constant is declared without the L suffix, the value overflows to negative on JVM/Native and the Swift adapter extracts a wrong alpha byte, causing colors to render incorrectly. Mitigation: the noColorConstantIsNegative() commonTest in Task 2 catches this on both JVM and iosSimulatorArm64 targets before any Swift code runs.
</threat_model>

<verification>
1. `./gradlew :shared-core:jvmTest --tests "dev.viethung.core.theme.DesignTokensTest"` — 3 tests, 0 failures
2. `./gradlew :shared-core:allTests` — green on JVM + iosSimulatorArm64 (Pitfall 6 cross-target gate)
3. `grep -c 'import ' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt` returns 0
4. `grep ': Long = 0x' shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt | grep -vc 'L'` returns 0 (all Long constants have L suffix)
</verification>

<success_criteria>
- DesignTokens.kt exists at `shared-core/src/commonMain/kotlin/dev/viethung/core/theme/DesignTokens.kt` with zero imports
- LightColors and DarkColors each define 35 Long constants, all with L suffix
- TextStyleToken data class has exactly four primitive fields
- All 15 M3 typography roles defined; 7 spacing steps; 7 radius steps
- `./gradlew :shared-core:allTests` exits 0 on JVM and iosSimulatorArm64
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-token-bridge/02-01-SUMMARY.md`
</output>
